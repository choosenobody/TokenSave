{
  "name": "tokensave",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "npm run build && vitest run",
    "audit:openclaw": "node scripts/tokensave-audit.mjs"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^4.1.5"
  }
}
